<form method="post" action="{{ path('app_pizza_service_delete', {'id': pizza_service.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce service ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pizza_service.id) }}">
    <button class="btn">Supprimer</button>
</form>
