<form method="post" action="{{ path('app_pizza_service_template_delete', {'id': pizza_service_template.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet exemple ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pizza_service_template.id) }}">
    <button class="btn">Supprimer</button>
</form>
